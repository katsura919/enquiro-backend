# Test Environment Variables
NODE_ENV=test
PORT=5001
JWT_SECRET=test-jwt-secret-key-for-testing
JWT_EXPIRE=1h

# MongoDB Memory Server will provide the test database URL
# No need to set MONGODB_URI here
